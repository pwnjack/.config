{
  "planPath": "docs/superpowers/plans/2026-08-01-repo-engineering.md",
  "specPath": "docs/superpowers/specs/2026-08-01-repo-engineering-design.md",
  "tasks": [
    {
      "id": 1,
      "subject": "Task 1: The test runner",
      "status": "completed",
      "description": "**Goal:** `./test.sh` discovers both existing suites, runs them as subprocesses, and reports a roll-up; `--list` and `--for` work.\n\n**Files:**\n- Create: `test.sh`\n- Test: `test/test-runner.sh`\n\n**Acceptance Criteria:**\n- [ ] `./test.sh --list` prints `scripts/doctor/test/run-tests.sh`, `scripts/waybar/test-battery.sh` and `test/test-runner.sh`, with owners `scripts/doctor/`, `scripts/waybar/` and `(repo root)`\n- [ ] The doctor's six `test-*.sh` fragments are NOT listed as entry points\n- [ ] `./test.sh` exits 0 and runs every suite\n- [ ] A failing suite makes `./test.sh` exit 1 and prints that suite's full captured output\n- [ ] `./test.sh --for rofi/powermenu.sh` exits 0 and runs only `test/test-runner.sh` (repo-root owner), not the doctor or battery suites\n- [ ] In a fixture with no repo-root suite, an uncovered `--for` prints \"no suites cover the changed files\" and exits 0\n- [ ] `./test.sh --help` prints usage and exits 0; an unknown option exits 2\n- [ ] A non-git `TEST_ROOT` exits 1 with a message naming git as the source of discovery\n- [ ] `shellcheck -S warning test.sh test/test-runner.sh` is silent\n\n**Verify:** `bash test/test-runner.sh` -> ends with `test.sh: N passed, 0 failed`, exit 0\n\n**Steps:** See the plan document for full code. Two traps it documents: `run()` must set `RUN_OUT`/`RC` in the parent, never via command substitution; and a passing suite's output is never printed, so assert on roll-up counts.\n\n```json:metadata\n{\"files\": [\"test.sh\", \"test/test-runner.sh\"], \"verifyCommand\": \"bash test/test-runner.sh && ./test.sh\", \"acceptanceCriteria\": [\"--list shows 3 suites with correct owners\", \"doctor fragments excluded\", \"./test.sh exits 0\", \"failing suite exits 1 and prints output\", \"--for selects by owning directory\", \"--help exits 0, unknown option exits 2\", \"non-git root exits 1\", \"shellcheck -S warning silent\"]}\n```"
    },
    {
      "id": 2,
      "subject": "Task 2: The doctor's waybar check",
      "status": "completed",
      "description": "**Goal:** `./doctor.sh` gains a Waybar group that reports unconfigured modules, orphaned config blocks, and handler binaries that are not installed.\n\n**Files:**\n- Create: `scripts/doctor/checks/waybar.sh`\n- Modify: `doctor.sh:51`, `doctor.sh:70`\n- Test: `scripts/doctor/test/test-waybar.sh`\n\n**Acceptance Criteria:**\n- [ ] A `custom/*` module placed with no config block is an ERROR\n- [ ] A built-in module placed with no config block is a WARN\n- [ ] A config block placed in no `modules-*` list is an INFO\n- [ ] A handler binary not in `PATH` is a WARN\n- [ ] `~/.config` and `$HOME/.config` handler paths are skipped -- `references.sh` owns them\n- [ ] The waybar action keywords produce no finding\n- [ ] Both the inline and multi-line `modules-*` array forms are parsed\n- [ ] Nested keys (`actions`, `format`) are not mistaken for config blocks\n- [ ] `ok` prints only when the check found nothing at all\n- [ ] On the live repo the group reports exactly one INFO -- the orphaned `user` block -- and `./doctor.sh` still exits 0\n\n**Verify:** `./scripts/doctor/test/run-tests.sh` -> `7 files, N assertions, 0 failures`, exit 0\n\n**Steps:** See the plan document for full code. All three extractors were validated against the live config.jsonc during design. Register the module in `doctor.sh:51` and `doctor.sh:70`.\n\n```json:metadata\n{\"files\": [\"scripts/doctor/checks/waybar.sh\", \"scripts/doctor/test/test-waybar.sh\", \"doctor.sh\"], \"verifyCommand\": \"./scripts/doctor/test/run-tests.sh && ./doctor.sh\", \"acceptanceCriteria\": [\"custom module without block is ERROR\", \"built-in without block is WARN\", \"unplaced block is INFO\", \"missing handler binary is WARN\", \"~/.config handlers skipped\", \"action keywords produce no finding\", \"both array forms parsed\", \"nested keys not read as blocks\", \"ok only on a clean config\", \"live doctor reports exactly one INFO and exits 0\"]}\n```"
    },
    {
      "id": 3,
      "subject": "Task 3: Hook wiring",
      "status": "completed",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** Committing a change under a suite's owning directory runs that suite, through `test.sh`, with no area names hardcoded in the hook.\n\n**Files:**\n- Modify: `scripts/hooks/pre-commit` (lines 28-33 and 49-58)\n\n**Acceptance Criteria:**\n- [ ] The hook contains no `^scripts/doctor/` literal\n- [ ] Staging a `scripts/waybar/` change runs the battery suite and not the doctor suite\n- [ ] Staging a `scripts/doctor/` change runs the doctor suite\n- [ ] Staging a `rofi/` change runs neither the doctor nor the battery suite and does not block the commit -- `test/test-runner.sh` owns the repo root and runs on every commit by design\n- [ ] A failing suite blocks the commit and prints that suite's output\n- [ ] `shellcheck -S warning scripts/hooks/pre-commit` is silent\n\n**Verify:** `shellcheck -S warning scripts/hooks/pre-commit` -> silent; the three commit probes in the plan behave as described\n\n**Steps:** Fill `STAGED_ALL` in the existing staged-file loop; replace the doctor-only stanza with a guarded `\"$REPO/test.sh\" --for \"${STAGED_ALL[@]}\"`. Then run the three commit probes, each followed by its documented undo.\n\n```json:metadata\n{\"files\": [\"scripts/hooks/pre-commit\"], \"verifyCommand\": \"shellcheck -S warning scripts/hooks/pre-commit\", \"acceptanceCriteria\": [\"no ^scripts/doctor/ literal in the hook\", \"waybar change runs the battery suite only\", \"doctor change runs the doctor suite\", \"unrelated change blocks nothing\", \"failing suite blocks the commit and prints output\", \"shellcheck silent\"]}\n```"
    },
    {
      "id": 4,
      "subject": "Task 4: Documentation",
      "status": "pending",
      "blockedBy": [
        1,
        2,
        3
      ],
      "description": "**Goal:** Every place that describes the test setup describes the one that now exists.\n\n**Files:**\n- Modify: `install.sh:291`\n- Modify: `README.md:135-140`\n- Modify: `CLAUDE.md` (Key Commands, doctor tree, Conventions)\n- Modify: `scripts/waybar/test-battery.sh:5-9`\n\n**Acceptance Criteria:**\n- [ ] No tracked file claims the hook runs only \"doctor tests\"\n- [ ] `CLAUDE.md` Key Commands lists `./test.sh`\n- [ ] `CLAUDE.md`'s doctor tree includes `waybar.sh` and `test-waybar.sh`\n- [ ] `CLAUDE.md` states the discovery convention, including the repo-root owner consequence\n- [ ] `test-battery.sh`'s header records the answer to the question it poses\n- [ ] `./doctor.sh` reports no new reference findings from the edited docs\n\n**Verify:** `grep -rn \"doctor tests\\|doctor's test suite\" install.sh README.md CLAUDE.md` -> no output\n\n**Steps:** See the plan document for the exact before/after text of each of the six edits.\n\n```json:metadata\n{\"files\": [\"install.sh\", \"README.md\", \"CLAUDE.md\", \"scripts/waybar/test-battery.sh\"], \"verifyCommand\": \"grep -rn 'doctor tests' install.sh README.md CLAUDE.md; ./doctor.sh; ./test.sh\", \"acceptanceCriteria\": [\"no stale 'doctor tests' claim remains\", \"CLAUDE.md lists ./test.sh\", \"doctor tree includes waybar.sh and test-waybar.sh\", \"discovery convention documented\", \"test-battery.sh header answers its own question\", \"doctor reports no new reference findings\"]}\n```"
    }
  ],
  "lastUpdated": "2026-08-01T21:38:23.459193+00:00"
}
