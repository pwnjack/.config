# Transitional placeholder for a Hyprland session started with Hyprlang.
#
# Hyprland 0.55 and 0.56 prefer hyprland.lua when it exists, but choose the
# configuration language only once at startup. The currently running legacy
# session recreates this path when it is deleted. On the next login Hyprland
# loads hyprland.lua instead; 0.57 ignores this placeholder entirely.
